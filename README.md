# airflow-test

## usage

```
export DOCKER_HOST_DB_PORT 33006
export AWS_ACCESS_KEY_ID XXXXXXX
export AWS_SECRET_ACCESS_KEY XXXXXXXXXXXXX
docker-compose build
docker-compose up -d
open http://localhost:8080 # 初回はdb構築があるのでちょっと時間かかるかも
```
